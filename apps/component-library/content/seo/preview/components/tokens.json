{
  "category": "Design tokens",
  "title": "Zentauri UI design token reference",
  "description": "Reference the global --zui-* CSS variables exported from src/design-system/tokens.ts and learn which values to override for theming Zentauri UI.",
  "keywords": [
    "Zentauri UI design tokens",
    "--zui CSS variables",
    "CSS variable theming",
    "Tailwind CSS v4 tokens",
    "React component theming",
    "focus ring token",
    "radius token"
  ],
  "og": {
    "title": "Design tokens · Zentauri UI",
    "description": "Global --zui-* CSS variables, dark-mode pairs, and fallback values generated from src/design-system/tokens.ts.",
    "type": "website"
  },
  "twitter": {
    "title": "Design tokens · Zentauri UI",
    "description": "Global --zui-* CSS variables, dark-mode pairs, and fallback values generated from src/design-system/tokens.ts.",
    "card": "summary_large_image"
  },
  "canonicalPath": "/preview/tokens",
  "headings": {
    "h1": "Design token reference",
    "h2": [
      "Token contract",
      "Theming strategy",
      "Common use cases",
      "FAQ"
    ]
  },
  "intro": "A standalone map of the global --zui-* CSS variable contract so consumers know which foundation tokens to override before tuning individual components.",
  "useCases": [
    "Set brand focus-ring colors across every interactive primitive.",
    "Align light and dark mode ring offsets with an app shell background.",
    "Standardize large-radius surfaces without editing component source.",
    "Document a product theme for downstream teams that vendor components with the CLI."
  ],
  "faqs": [
    {
      "question": "Are these the only CSS variables Zentauri UI supports?",
      "answer": "No. This page covers global tokens from src/design-system/tokens.ts. Component pages include their own CSS variable sections for component-specific appearances, slots, and states."
    },
    {
      "question": "Do I need to override dark variables?",
      "answer": "Only when your dark theme should differ from the shipped fallback. Dark tokens use the same name with -dark appended."
    },
    {
      "question": "Where should overrides live?",
      "answer": "Place broad theme overrides on :root and .dark, or scope them to a wrapper class when a product surface needs a local theme."
    }
  ],
  "sections": [
    {
      "heading": "Token contract",
      "body": "Zentauri UI exposes theme controls as CSS variables prefixed with --zui-. Component class strings include hardcoded fallbacks, so a missing override still renders with the library default.\n\nThe global token source is src/design-system/tokens.ts. This docs page reads those exported constants and derives the visible reference table from the CSS variable declarations inside them."
    },
    {
      "heading": "Theming strategy",
      "body": "Start with global tokens for focus rings, ring offsets, and shared radius. Then override component-level variables on each component page when you need appearance-specific colors, borders, shadows, or hover states.\n\nKeep light and dark values paired so Tailwind scans both class variants and your theme remains predictable across mode changes."
    },
    {
      "heading": "Common use cases",
      "body": ""
    }
  ],
  "useCasesSectionHeading": "Common use cases"
}
