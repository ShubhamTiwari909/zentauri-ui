{
  "category": "Design tokens",
  "title": "Zentauri UI design token reference",
  "description": "Reference the global --zui-* CSS variables exported from src/design-system/tokens.ts and learn how to override brand, surface, status, and palette tokens for theming Zentauri UI.",
  "keywords": [
    "Zentauri UI design tokens",
    "--zui CSS variables",
    "CSS variable theming",
    "Tailwind CSS v4 tokens",
    "React component theming",
    "brand color palette",
    "global theme tokens",
    "focus ring token",
    "radius token"
  ],
  "og": {
    "title": "Design tokens · Zentauri UI",
    "description": "Global --zui-* CSS variables, dark-mode pairs, palette examples, and fallback values generated from src/design-system/tokens.ts.",
    "type": "website"
  },
  "twitter": {
    "title": "Design tokens · Zentauri UI",
    "description": "Global --zui-* CSS variables, dark-mode pairs, palette examples, and fallback values generated from src/design-system/tokens.ts.",
    "card": "summary_large_image"
  },
  "canonicalPath": "/preview/tokens",
  "headings": {
    "h1": "Design token reference",
    "h2": [
      "Token contract",
      "Override globals first",
      "Theming strategy",
      "Common use cases",
      "FAQ"
    ]
  },
  "intro": "A standalone map of the global --zui-* CSS variable contract so consumers know which brand, surface, status, and palette tokens to override before tuning individual components.",
  "useCases": [
    "Set one brand color palette across every interactive primitive.",
    "Generate broad light and dark themes without hand-authoring component variables.",
    "Use component-specific variables only when a single component needs a custom override.",
    "Set brand focus-ring colors across every interactive primitive.",
    "Align light and dark mode ring offsets with an app shell background.",
    "Standardize large-radius surfaces without editing component source.",
    "Document a product theme for downstream teams that vendor components with the CLI."
  ],
  "faqs": [
    {
      "question": "Are these the only CSS variables Zentauri UI supports?",
      "answer": "No. This page focuses on global tokens from src/design-system/tokens.ts. Component pages include their own CSS variable sections for component-specific appearances, slots, and states. Component tokens still win when both are set."
    },
    {
      "question": "Do I need to override dark variables?",
      "answer": "Only when your dark theme should differ from the shipped fallback. Dark tokens use the same name with -dark appended."
    },
    {
      "question": "Where should overrides live?",
      "answer": "Place broad theme overrides on :root and .dark, or scope them to a wrapper class when a product surface needs a local theme."
    },
    {
      "question": "Should I override global tokens or component tokens?",
      "answer": "Start with global tokens for brand, foreground, surface, border, status, and palette values. Add component-level overrides only for deliberate exceptions such as a custom button appearance or a special alert tone."
    }
  ],
  "sections": [
    {
      "heading": "Token contract",
      "body": "Zentauri UI exposes theme controls as CSS variables prefixed with --zui-. Component class strings include hardcoded fallbacks, so a missing override still renders with the library default.\n\nThe global token source is src/design-system/tokens.ts. This docs page reads those exported constants and shows the foundation tokens that component variables use as their fallback layer."
    },
    {
      "heading": "Override globals first",
      "body": "Set brand, foreground, surface, border, status, and palette variables on :root and .dark to create a full product theme. Component variables such as --zui-button-default-bg remain available for exceptions on each component preview page, but most teams can theme broad surfaces with the smaller global token set."
    },
    {
      "heading": "Theming strategy",
      "body": "Start with global tokens for brand colors, status colors, surfaces, borders, focus rings, ring offsets, shadows, and shared radius. Then override component-level variables on each component page when you need appearance-specific colors, borders, shadows, or hover states.\n\nKeep light and dark values paired so Tailwind scans both class variants and your theme remains predictable across mode changes."
    },
    {
      "heading": "Common use cases",
      "body": ""
    }
  ],
  "useCasesSectionHeading": "Common use cases"
}
