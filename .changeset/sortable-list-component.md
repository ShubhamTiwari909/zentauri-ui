---
"@zentauri-ui/zentauri-components": minor
---

Add the SortableList component: a reorderable list with native drag-and-drop, accessible move buttons, and modifier+arrow keyboard reordering that stays available when the buttons are hidden. Ships the `--zui-sortable-list-*` token contract across 15 appearances plus `glass` and three gradients, in sm/md/lg sizes.
